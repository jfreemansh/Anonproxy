name: Build macOS app

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: macos-14, arch: arm64,  pbsarch: aarch64-apple-darwin }
          - { os: macos-13, arch: x86_64, pbsarch: x86_64-apple-darwin }
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Fetch embedded Python runtime
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          URL=$(gh api repos/astral-sh/python-build-standalone/releases/latest \
                --jq '.assets[].browser_download_url' \
                | grep "cpython-3.12.*${{ matrix.pbsarch }}-install_only.tar.gz" | head -1)
          echo "runtime: $URL"
          mkdir -p /tmp/pbs && curl -sL "$URL" | tar xz -C /tmp/pbs

      - name: Assemble Anonbar.app (${{ matrix.arch }})
        run: scripts/build_app.sh
        env:
          RUNTIME_DIR: /tmp/pbs/python

      - name: Zip + checksum
        run: |
          cd build
          ditto -c -k --keepParent Anonbar.app "Anonbar-${{ matrix.arch }}.zip"
          shasum -a 256 "Anonbar-${{ matrix.arch }}.zip" > "Anonbar-${{ matrix.arch }}.zip.sha256"

      - uses: actions/upload-artifact@v4
        with:
          name: Anonbar-${{ matrix.arch }}
          path: build/Anonbar-*

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with: { pattern: "Anonbar-*", merge-multiple: true, path: rel }
      - uses: softprops/action-gh-release@v2
        with:
          files: rel/*
